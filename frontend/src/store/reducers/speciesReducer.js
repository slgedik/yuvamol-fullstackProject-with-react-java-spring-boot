/*import { SET_SPECIES } from "../actions/speciesAction";
const initialState = {
    species: [],
  };
  
  const speciesReducer = (state = initialState, action) => {
    switch (action.type) {
      case SET_SPECIES:
        return { ...state, animals: action.payload };
      default:
        return state;
    }
  };
  
  export default speciesReducer; */